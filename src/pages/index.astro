---
// Home page - demonstrating SSG (Static Site Generation)
import Layout from '../layouts/Layout.astro';
import Hero from '../components/Hero.astro';
import FeatureGrid from '../components/FeatureGrid.astro';
import Card from '../components/Card.astro';

// This page will be statically generated at build time (SSG)
// Data fetching happens at build time
const currentYear = new Date().getFullYear();
---

<Layout title="Home - Astro Learning Blog">
  <!-- Hero Section -->
  <Hero 
    title="Learn Astro by Building"
    subtitle="Master components, props, SSR, SSG, and modern web development patterns"
    ctaText="Explore the Blog"
    ctaLink="/blog"
  >
  </Hero>

  <!-- Quick Start Section -->
  <section class="py-16 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-12">
      <h2 class="text-3xl font-bold text-gray-900 mb-4">🚀 Quick Start Learning</h2>
      <p class="text-lg text-gray-600 max-w-2xl mx-auto">
        Jump into hands-on learning with these key concepts
      </p>
    </div>
    
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
      <Card title="📚 Components & Props" href="/about" hover={true}>
        <p class="text-gray-600 mb-4">
          Learn how to create reusable components and pass data using props. 
          See examples of TypeScript interfaces and component composition.
        </p>
        <div class="flex flex-wrap gap-2">
          <span class="bg-blue-100 text-blue-800 px-2 py-1 rounded text-sm">TypeScript</span>
          <span class="bg-green-100 text-green-800 px-2 py-1 rounded text-sm">Props</span>
          <span class="bg-purple-100 text-purple-800 px-2 py-1 rounded text-sm">Slots</span>
        </div>
      </Card>

      <Card title="📝 Dynamic Content" href="/blog" hover={true}>
        <p class="text-gray-600 mb-4">
          Explore how to fetch data from APIs, create dynamic routes, and implement 
          both SSG and SSR patterns.
        </p>
        <div class="flex flex-wrap gap-2">
          <span class="bg-orange-100 text-orange-800 px-2 py-1 rounded text-sm">API Fetch</span>
          <span class="bg-indigo-100 text-indigo-800 px-2 py-1 rounded text-sm">SSG</span>
          <span class="bg-pink-100 text-pink-800 px-2 py-1 rounded text-sm">Dynamic Routes</span>
        </div>
      </Card>

      <Card title="⚡ Performance" href='/contact' hover={true}>
        <p class="text-gray-600 mb-4">
          Discover Astro's zero-JavaScript runtime, partial hydration, and 
          island architecture for optimal performance.
        </p>
        <div class="flex flex-wrap gap-2">
          <span class="bg-red-100 text-red-800 px-2 py-1 rounded text-sm">Zero JS</span>
          <span class="bg-yellow-100 text-yellow-800 px-2 py-1 rounded text-sm">Islands</span>
          <span class="bg-cyan-100 text-cyan-800 px-2 py-1 rounded text-sm">Fast</span>
        </div>
      </Card>
    </div>
  </section>

  <!-- Features Section -->
  <FeatureGrid />

  <!-- Code Example Section -->
  <section class="py-16 bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-12">
        <h2 class="text-3xl font-bold text-gray-900 mb-4">🛠️ See It in Action</h2>
        <p class="text-lg text-gray-600">
          This page demonstrates SSG - it's generated at build time ({currentYear})
        </p>
      </div>
      

      
      <div class="mt-8 text-center">
        <a 
          href="/blog"
          class="inline-flex items-center bg-indigo-600 text-white px-6 py-3 rounded-lg font-semibold hover:bg-indigo-700 transition-colors"
          data-astro-prefetch="hover"
        >
          See SSR in Action
          <svg class="ml-2 w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
          </svg>
        </a>
      </div>
    </div>
  </section>
</Layout>
